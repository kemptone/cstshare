---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
const base = import.meta.env.BASE_URL;
---

<Layout title="Blog - CSTShare">
  <div class="max-w-4xl mx-auto space-y-8">
    <div class="text-center">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">Blog</h1>
      <p class="text-lg text-gray-600">
        Production tips, tutorials, and community stories
      </p>
    </div>

    {posts.length === 0 ? (
      <div class="text-center py-12 bg-gray-50 rounded-lg">
        <p class="text-gray-600 text-lg">No blog posts yet. Check back soon!</p>
      </div>
    ) : (
      <div class="space-y-6">
        {posts.map((post) => (
          <article class="bg-white p-6 rounded-lg shadow-sm border border-gray-200 hover:shadow-md transition">
            <a href={`${base}blog/${post.slug}`} class="block">
              <h2 class="text-2xl font-bold text-gray-900 mb-2 hover:text-blue-600 transition">
                {post.data.title}
              </h2>
              <div class="flex items-center gap-4 text-sm text-gray-500 mb-3">
                <span>{post.data.author}</span>
                <span>•</span>
                <time datetime={post.data.date.toISOString()}>
                  {post.data.date.toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                  })}
                </time>
              </div>
              <p class="text-gray-600 mb-4">{post.data.description}</p>
              {post.data.tags && post.data.tags.length > 0 && (
                <div class="flex flex-wrap gap-2">
                  {post.data.tags.map((tag) => (
                    <span class="text-xs bg-blue-100 text-blue-700 px-3 py-1 rounded-full">
                      {tag}
                    </span>
                  ))}
                </div>
              )}
            </a>
          </article>
        ))}
      </div>
    )}
  </div>
</Layout>
